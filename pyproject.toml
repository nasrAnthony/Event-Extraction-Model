[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "event-extraction-model"
version = "0.0.0"
description = "Event extraction demo package"
authors = [
  { name = "Christine" }
]
